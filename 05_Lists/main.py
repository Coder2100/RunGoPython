#initialize the list
numbers = [0] * 5

# change one of the them
numbers[2] = 100

getNumbers = numbers[1:3]
#output the numbers
print(getNumbers)

#length of it
print(len(getNumbers))

# initialize another list
scores = []
scores.append(1.1)

scores[0] = 2.2
print(scores)
